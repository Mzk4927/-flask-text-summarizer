# 📄 TextRank Summarizer – Flask on Hugging Face Spaces

> Paste **plain text**, a **local file path** (`.txt`, `.docx`, `.pdf`) **or a URL** and instantly get the most important sentences extracted with the classic **TextRank** algorithm (PageRank over cosine TF-IDF similarity).

---

### 🚀 Live Demo  
https://huggingface.co/spaces/`<your-username>`/textrank-flask  
*(Replace `<your-username>` with your HF username.)*

---

### 🧪 Local Quick-Start
```bash
# Clone & enter
git clone https://huggingface.co/spaces/<your-username>/textrank-flask
cd textrank-flask

# One-liner install
pip install -r requirements.txt
python -m spacy download en_core_web_sm

# Launch
python app.py